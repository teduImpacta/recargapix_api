export enum ProductType {
    transport = 'Transport',
    cellphone = 'Cellphone',
    store = 'Store',
    internet = 'Internet'
}

export enum RechargeStatus {
    pending = 'Pending',
    paid = 'Paid',
    canceled = 'Canceled'
}
