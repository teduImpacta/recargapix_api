export enum ProductType {
    transport = 'Transport',
    cellphone = 'Cellphone',
    store = 'Store'
}

export enum RechargeStatus {
    pending = 'Pending',
    paid = 'Paid',
    canceled = 'Canceled'
}
